data/
node_modules/
workspace-app/node_modules/

# Generated by `npm run build` (workspace-app's Vite build) — regenerated fresh on
# every Railway deploy, never hand-edited, so it doesn't belong in git.
public/workspace.html
public/workspace-assets/
